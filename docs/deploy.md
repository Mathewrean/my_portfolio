# Deploy Notes

This is a static site and can be deployed on GitHub Pages, Netlify, or Vercel.

## GitHub Pages

1. Push files to your repository root.
2. In repository settings, enable Pages from the `main` branch root.
3. Keep `.nojekyll` if serving as plain static files.

## Domain

Keep `CNAME` updated with your custom domain if used.
